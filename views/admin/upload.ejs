<%- include('../partials/head', { title, bodyClass: 'admin-body' }) %>
<%- include('../partials/admin-nav') %>

<main class="admin-main">
  <h1 class="admin-page-title"><%= t.admin.nav.upload %></h1>

  <div class="upload-zone" id="upload-zone">
    <div class="upload-zone__inner">
      <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
        <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/>
        <polyline points="17 8 12 3 7 8"/>
        <line x1="12" y1="3" x2="12" y2="15"/>
      </svg>
      <p><%= t.admin.upload.dragHint %> <label class="upload-zone__browse" for="file-input"><%= t.admin.upload.browse %></label></p>
      <p class="upload-zone__hint"><%= t.admin.upload.hint %></p>
    </div>
    <input type="file" id="file-input" accept="image/jpeg,image/png" multiple hidden>
  </div>

  <div class="upload-list" id="upload-list"></div>

  <template id="upload-item-tpl">
    <div class="upload-item" data-file-id="">
      <img class="upload-item__preview" src="" alt="">
      <div class="upload-item__body">
        <p class="upload-item__filename"></p>
        <div class="upload-item__fields">
          <div class="form-group">
            <label class="form-label"><%= t.admin.upload.titleField %></label>
            <input class="form-input" type="text" name="title" placeholder="Opcional">
          </div>
          <div class="form-group">
            <label class="form-label"><%= t.admin.upload.location %></label>
            <input class="form-input" type="text" name="location" placeholder="Opcional">
          </div>
          <div class="form-group">
            <label class="form-label"><%= t.admin.upload.date %></label>
            <input class="form-input" type="date" name="shotAt">
          </div>
          <div class="form-group">
            <label class="form-label">Colección</label>
            <select class="form-input" name="collectionId">
              <option value=""><%= t.admin.upload.noCollection %></option>
              <% collections.forEach(col => { %>
                <option value="<%= col.id %>"><%= col.name %></option>
              <% }) %>
            </select>
          </div>
          <div class="form-group form-group--full">
            <label class="form-label"><%= t.admin.upload.tags %></label>
            <input class="form-input" type="text" name="tags" placeholder="paisaje, panama, calle">
          </div>
        </div>
        <div class="upload-item__progress" hidden>
          <div class="progress-bar">
            <div class="progress-bar__fill" style="width: 0%"></div>
          </div>
          <span class="upload-item__status"></span>
        </div>
      </div>
      <button class="upload-item__remove btn btn--ghost btn--sm" type="button">✕</button>
    </div>
  </template>

  <div class="upload-footer" id="upload-footer" hidden>
    <button class="btn btn--primary" id="upload-all-btn"><%= t.admin.upload.uploadAll %></button>
  </div>
</main>

<script type="application/json" id="upload-i18n">
<%- JSON.stringify({
  getting: t.admin.upload.getting,
  uploading: t.admin.upload.uploading,
  processing: t.admin.upload.processing,
  done: t.admin.upload.done,
  errStart: t.admin.upload.errStart,
  errS3: t.admin.upload.errS3,
  errComplete: t.admin.upload.errComplete,
  onlyJpegPng: t.admin.upload.onlyJpegPng,
  maxSize: t.admin.upload.maxSize,
}).replace(/</g, '\\u003c') %>
</script>

<script type="module" src="/dist/upload.js"></script>
<script type="module" src="/dist/admin.js"></script>
</body>
</html>
